{
    "name": "opencat/translation-memory",
    "description": "SQLite-backed translation memory with fuzzy matching for the OpenCAT Framework",
    "type": "library",
    "version": "0.1.0",
    "require": {
        "php": "^8.2",
        "ext-pdo": "*",
        "ext-pdo_sqlite": "*",
        "ext-intl": "*",
        "ext-mbstring": "*",
        "opencat/core": "^0.1",
        "opencat/tmx": "^0.1"
    },
    "suggest": {
        "ext-pdo_pgsql": "Required for PostgresTranslationMemory — also needs pg_trgm extension enabled in the database"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "autoload": {
        "psr-4": {
            "OpenCat\\TranslationMemory\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "OpenCat\\TranslationMemory\\Tests\\": "tests/"
        }
    },
    "repositories": [
        {
            "type": "path",
            "url": "../core"
        },
        {
            "type": "path",
            "url": "../tmx"
        }
    ]
}
